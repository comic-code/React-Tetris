import styled from "styled-components";

export const MenuWrapper = styled.section`
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 50vh;
  z-index: 0;

  button {
    padding: 2.5rem 5rem;
    font-size: 2rem;
    border-radius: 20px;
    border: none;
    box-shadow: rgba(0, 0, 0, 0.5) 0px 0px 3.7rem;
    cursor: pointer;
  }
`